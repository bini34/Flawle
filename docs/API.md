# API

This document will describe the main API endpoints under `/api/v1/`.

- /api/v1/customer/ -> Mobile customer endpoints
- /api/v1/seller/ -> Seller/ERP endpoints

(Expand with endpoints, request/response examples)
