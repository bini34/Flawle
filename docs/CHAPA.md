# CHAPA Integration

Add Chapa (Ethiopian payment gateway) integration notes here: init, verify, webhook endpoints.

- CHAPA_API_KEY: set in env
- Payment flow: create payment -> redirect to Chapa -> webhook to verify
